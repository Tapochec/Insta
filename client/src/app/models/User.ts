export interface User { //используется для создания моделм юзера (ну как класс User  джава)
  id: number;
  email: string;
  username: string;
  firstname: string;
  lastname: string;
  bio: string;
}
